services:
  - type: web
    name: sales-forecast
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app_simple.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true
```

---

## 📝 **Update `requirements.txt` - Add specific versions:**
```
pandas==2.1.4
numpy==1.26.2
scikit-learn==1.3.2
streamlit==1.39.0
plotly==5.18.0
python-dateutil==2.8.2
```

---

## ⚙️ **In Render Dashboard, set these MANUALLY:**

**Build Command:**
```
pip install --upgrade pip && pip install -r requirements.txt
```

**Start Command:**
```
streamlit run app_simple.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true --server.enableCORS=false --server.enableXsrfProtection=false
